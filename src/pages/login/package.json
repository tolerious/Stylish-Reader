{
  "name": "popup",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx tailwindcss -i ./src/style.css -o ./src/output.css && tsc && vite build && cp dist/* ../",
    "preview": "vite preview",
    "watch": "npx tailwindcss -i ./src/style.css -o ./src/output.css --watch"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.30",
    "tailwindcss": "^3.4.6",
    "typescript": "^5.2.2",
    "vite": "^5.3.4"
  },
  "dependencies": {
    "jquery": "^3.7.1"
  }
}
